function createGrid(size) {}

function styleDiv(divToStyle, sizeOfPixels) {
  divToStyle.style.width = sizeOfPixels + "px";
  divToStyle.style.height = sizeOfPixels + "px";
  divToStyle.classList.add("grid");
}
